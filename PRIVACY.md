# A 隐私与用户数据说明

A 是本地桌面应用。当前个人魔改版的主要数据来源是用户自己的 Navidrome 服务器和本地音乐文件夹；应用不会把这些内容自动上传到 GitHub 或其他第三方服务。

## 本地保存的数据

应用可能在本机用户数据目录或浏览器本地存储中保存：

- Navidrome 服务器地址、账号名称和当前账号配置。
- Navidrome 密码、Token 或 API Key 的加密凭据。凭据通过 Electron `safeStorage` 保存，不以明文写入前端配置。
- 本地音乐库的文件夹索引、音频标签、封面缓存和歌词缓存。
- 播放队列、最近播放、播放历史和搜索历史。
- 自定义专辑封面、歌词布局、视觉控制设置和用户存档。
- 本地节奏分析及其他用于改善播放体验的缓存。

本地音乐索引只记录扫描结果和文件引用，音乐文件本身不会被复制到仓库或自动上传。

## 网络请求

- Navidrome 请求只发送到用户配置的 Navidrome 地址，用于连接测试、读取专辑/歌单、获取封面和歌词，以及播放音频。
- 播放和封面请求经过 A 的本地服务代理，以处理鉴权和浏览器播放限制。
- GitHub Actions 只用于仓库代码的 Windows 构建，不会读取开发机上的 Navidrome 配置、本地音乐或用户缓存。
- 更新检查（如果启用）只读取仓库配置的公开版本信息，不应上传用户音乐内容或账号凭据。

## 不应上传到 GitHub 的内容

以下内容绝对不要提交到仓库：

- Navidrome 密码、Token、API Key、Cookie 或私密访问地址。
- `.cookie`、`.qq-cookie` 以及其他平台登录状态文件。
- `node_modules/`、Electron 打包产物、本地日志和调试转储。
- 用户上传的音乐文件、歌词文件、封面和本地媒体索引。
- 包含账号信息、私密路径或播放记录的截图和配置备份。

## 旧平台兼容代码

仓库中仍保留部分原项目的平台兼容后端。当前 A 默认界面不要求这些平台账号；如果用户主动启用相关兼容入口，产生的 Cookie 或登录状态仍只应保存在本机，并应按照对应平台的用户协议使用。

A 不提供绕过付费、绕过会员、破解音质或重新分发音乐内容的能力。请用户自行确认 Navidrome 服务器、音乐文件和第三方服务的授权情况。
